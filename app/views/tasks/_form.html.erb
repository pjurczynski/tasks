<%= form_for(@task) do |f| %>
  <% if @task.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@task.errors.count, "error") %> prohibited this task from being saved:</h2>

      <ul>
      <% @task.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :desc, "Zadanie" %>
    <%= f.text_area :desc, :rows => 3 %>
  </div>
  <div class="field">
    <%= f.label :person_id, "Dla" %>
    <%= f.select :person_id, @people.map { |p| [p.name, p.id]} %>
  </div>
  <div class="field">
    <%= f.label :date_start, "Rozpoczecie" %>
    <%= f.date_select :date_start %>
  </div>
  <div class="actions">
    <%= f.hidden_field :active, :value=>true %>
    <%= f.submit 'utwórz' %>
  </div>
<% end %>
